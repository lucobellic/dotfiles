monitor = , preferred, auto, 1

# Disable logind handle-lid-switch to manage it manually /etc/systemd/logind.conf
# HandleLidSwitch=ignore
# HandleLidSwitchExternalPower=ignore
# HandleLidSwitchDocked=ignore

bindl = , switch:on:Lid Switch, exec, lid_monitor_handler.rs close
bindl = , switch:off:Lid Switch, exec, lid_monitor_handler.rs open

exec = setup_monitors.rs
